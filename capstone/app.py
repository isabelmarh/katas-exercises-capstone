def run_query(query: str) -> str:
    """Temporary orchestration entry point for the capstone app."""
    return (
        "Scaffold ready. Next, implement routing, retrieval, coaching, memory, "
        f"and guardrails for query: {query}"
    )
