[project]
name = "weather-api-mcp"
version = "0.1.0"
description = "MCP server for weather data using wttr.in"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=0.2.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
