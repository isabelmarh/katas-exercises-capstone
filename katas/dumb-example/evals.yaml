# yaml-language-server: $schema=../../evals-schema.json
cases:
  - name: simple_test
    inputs: "What is 2 + 2?"
    expected_output: "4"
    metadata:
      difficulty: easy
    evaluators:
      - EqualsExpected
